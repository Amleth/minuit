- Il faut distinguer, car on ne va pas les appeler de la même manière :

(10*f) : fonction appelée dix fois qui génère un symbole à chaque appel
(10:f) : fonction appelée une fois qui génère une liste de 10 symboles

Dans les deux cas, on sait que le parser va devoir réserver 10 places pour mettre les symboles

- n-olets